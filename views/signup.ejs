<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Node Blog | Create Account, Join the node blog today</title>
  <link rel="stylesheet" href="assets/css/diko.css">
</head>

<body>
  <%- include('./partials/nav') %>
  <section class="one py-4">
    <div class="row djcc pt-3 pt-md-5">
      <div class="col-12 col-md-6 rounded col-lg-4 p-3 dflex dfdc shadow-lg ">
        <h4>Join NodeBlog today</h4>
        <form action="/signup" method="post" class="w-100">
          <div class="my-3">
            <% if (locals.error) { %>
              <div class=" py-2 px-3 rounded w-100 alert-danger">
                <%= locals.error %>
              </div>
              <% }else if (locals.success){ %>
                <div class=" py-2 px-3 rounded w-100 alert-success">
                  <%= locals.success %>
                </div>
                <% } %>
          </div>
          <div class="my-3">
            <input value="<%= locals.username %>" required type="text" name="username" id="username" placeholder="Enter username Here"
              class="border border-secondary bg3 py-2 px-3 rounded w-100">
          </div>
          <div class="my-3">
            <input value="<%= locals.email %>" required type="email" name="email" id="email"
              placeholder="Enter Email Here" class="border border-secondary bg3 py-2 px-3 rounded w-100">
          </div>
          <div class="my-3">
            <input value="<%= locals.password %>" required type="password" name="password" id="password"
              placeholder="Enter password Here" class="border border-secondary bg3 py-2 px-3 rounded w-100">
          </div>
          <div class="my-3">
            <input type="hidden" name="action" value="signup" hidden>
            <input type="submit" name="submit" id="submit" value="Create Account"
              class="border border-secondary bg-dark text-light py-2 px-3 rounded w-100">
          </div>
          <div class="my-3 dflex djcsb">
            <a href="/login" class="btn-link">Login Instead</a>
            <a href="/recover" class="btn-link">Forgot Password</a>
          </div>
        </form>
      </div>
    </div>
  </section>
</body>

</html>